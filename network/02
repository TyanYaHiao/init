fconfig | grep "broadcast" | awk '{print $6}'

# Identify and display the Ethernet interface characteristics:
# (a) Identify broadcast address

arp -a

# (b) Identify all IP adresses which are part of the same subnet
